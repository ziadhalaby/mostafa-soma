const eventDate = new Date('2026-09-02T20:00:00+03:00');
const envelope = document.getElementById('envelope');
document.getElementById('openInvitation').addEventListener('click', () => { envelope.classList.add('opened'); document.getElementById('website').hidden = false; setTimeout(() => document.querySelector('.hero').scrollIntoView(), 80); });
function updateCountdown(){let r=Math.max(0,eventDate-Date.now()),v=[Math.floor(r/864e5),Math.floor(r%864e5/36e5),Math.floor(r%36e5/6e4),Math.floor(r%6e4/1e3)];['days','hours','minutes','seconds'].forEach((id,i)=>document.getElementById(id).textContent=String(v[i]).padStart(2,'0'))}updateCountdown();setInterval(updateCountdown,1000);
const dates=document.getElementById('calendarDates'),offset=(new Date(2026,8,1).getDay()+6)%7;for(let i=0;i<offset;i++)dates.insertAdjacentHTML('beforeend','<span class="blank">0</span>');for(let d=1;d<=30;d++)dates.insertAdjacentHTML('beforeend',`<span class="${d===2?'wedding-day':''}">${d}</span>`);
document.getElementById('addCalendar').addEventListener('click',e=>{e.preventDefault();const ics=['BEGIN:VCALENDAR','VERSION:2.0','BEGIN:VEVENT','DTSTART:20260902T170000Z','DTEND:20260902T210000Z','SUMMARY:Mostafa & Soma Wedding','LOCATION:أكاسيا — Acacia','URL:https://maps.app.goo.gl/GCQ5THdxBJFsDNi86?g_st=iw','END:VEVENT','END:VCALENDAR'].join('\r\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([ics],{type:'text/calendar'}));a.download='mostafa-soma-wedding.ics';a.click();URL.revokeObjectURL(a.href)});
const dialog=document.getElementById('rsvpDialog');document.getElementById('rsvpButton').addEventListener('click',()=>dialog.showModal());dialog.querySelector('.close').addEventListener('click',()=>dialog.close());document.getElementById('rsvpForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('formNote').textContent='Thank you — we look forward to celebrating with you!';e.target.reset()});
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.18});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
